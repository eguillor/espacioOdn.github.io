---
import "swiper/css/bundle";
import CardProfe from "./card-profe.astro";
import Profe from "../data/profesionales.json";
---

<section id="3" class="container my-5 py-5">
  <div class="text-center">
    <h3 class="text-primary">NUESTROS PROFESIONALES ODONTÓLOGOS</h3>
    <p>
      En <b class="text-primary">ESPACIO ODONTOLÓGICO</b> acompañamos a cada paciente
      con dedicación y cariño. Contamos con un equipo de especialistas que trabaja
      de manera integral, pensando siempre en lo mejor para vos. Porque tu bienestar
      y tu felicidad nos importan.
    </p>
  </div>
  <div class="row d-flex justify-content-center">
    <div
      class="swiper p-2 col-12 col-md-6 col-lg-4 wow animate__animated animate__flipInY animate__slower"
    >
      <div class="swiper-wrapper people">
        {
          Profe.slice(0, 3).map((profe) => {
            return (
              <CardProfe
                cardImgProfe={profe.cardImgProfe}
                cardNameProfe={profe.cardNameProfe}
              />
            );
          })
        }
      </div>
    </div>

    <div
      class="swipers p-2 col-12 col-md-6 col-lg-4 wow animate__animated animate__flipInY animate__slower"
    >
      <div class="swiper-wrapper people">
        {
          Profe.slice(3, 7).map((profe) => {
            return (
              <CardProfe
                cardImgProfe={profe.cardImgProfe}
                cardNameProfe={profe.cardNameProfe}
              />
            );
          })
        }
      </div>
    </div>

    <div
      class="swiperss p-2 col-12 col-md-6 col-lg-4 wow animate__animated animate__flipInY animate__slower"
    >
      <div class="swiper-wrapper people">
        {
          Profe.slice(7, 11).map((profe) => {
            return (
              <CardProfe
                cardImgProfe={profe.cardImgProfe}
                cardNameProfe={profe.cardNameProfe}
              />
            );
          })
        }
      </div>
    </div>
  </div>
</section>

<script>
  import Swiper from "swiper/bundle";

  const swiper = new Swiper(".swiper", {
    // Optional parameters
    effect: "flip",
    grabCursor: true,
    loop: true,

    // Enabled autoplay mode
    autoplay: {
      delay: 3500,
      disableOnInteraction: false,
    },
  });

  const swipers = new Swiper(".swipers", {
    // Optional parameters
    effect: "flip",
    grabCursor: true,
    loop: true,

    // Enabled autoplay mode
    autoplay: {
      delay: 3500,
      disableOnInteraction: false,
    },
  });

  const swiperss = new Swiper(".swiperss", {
    // Optional parameters
    effect: "flip",
    grabCursor: true,
    loop: true,

    // Enabled autoplay mode
    autoplay: {
      delay: 3500,
      disableOnInteraction: false,
    },
  });
</script>
